# MQTT Configuration (THM)
MQTT_BROKER_URL=iti-mqtt.mni.thm.de
MQTT_BROKER_PORT_WS=9001                 # WebSocket (frontend, web-controller)
MQTT_BROKER_PORT_TCP=1883                # TCP (Java-Backend)
MQTT_USERNAME=group-16
MQTT_PASSWORD=r%^e{[FxW8Lp
MQTT_MESSAGE_PREFIX=group-16/            # Topics: group-16/game/state, group-16/player/... (DesignVorschlag)

# Database Configuration
# This part is used to configure the database connection for the java-backend service.
DB_USER=user
DB_PASSWORD=userpassword
DB_ROOT_PASSWORD=rootpassword
DB_NAME=quiz_game
DB_HOST=mariadb
DB_PORT=3306
